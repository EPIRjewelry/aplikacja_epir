node_modules/
**/node_modules/
.git/
.cloudflare/
.wrangler/
.cache/
.tmp/
**/.vitest-cache/
*.log
audit_report.json
apps/kazka/public/build/
apps/zareczyny/public/build/
.env
.env.*
.dev.vars

# --- Kilo Code Encapsulation ---
.kilo/
kilo.jsonc

# --- Token budget / vector isolation ---
# Kompilacje Operator Studio (nie zrodlo)
workers/chat/operator-studio-dist/

# Zrzuty danych Shopify (Bulk Ops)
**/*.jsonl

# Binaria i statyki (bez blokowania kodu w extensions/*/assets/)
**/*.png
**/*.jpg
**/*.jpeg
**/*.wasm
# SVG: tylko public/ storefrontow (favicon); nie globalnie
apps/kazka/public/**/*.svg
apps/zareczyny/public/**/*.svg

# --- Blender 3D (binaria w repo) ---
*.blend
*.blend1
*.blend2
*.fbx
*.glb
*.gltf
*.obj
*.dae

# specs/, agents/, .github/agents/, .notes/ -> .cursorindexingignore (dostep przez @, bez auto-indeksu)
