# EPIR agents default mode: local-first
EPIR_AGENT_MODE=local

# Optional Framework mode (set real values before EPIR_AGENT_MODE=framework)
AZURE_AI_PROJECT_ENDPOINT=https://your-project.services.ai.azure.com/api/projects/your-project-name
AZURE_AI_MODEL_DEPLOYMENT_NAME=your-model-deployment-name
