name: PR Triage Labeler

# Auto-etykiety wyłączone. Przywróć `on: pull_request_target` z historii Gita, jeśli potrzebne.
on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  disabled-notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Labeler wyłączony — etykiety ustawiaj ręcznie lub przywróć workflow."
