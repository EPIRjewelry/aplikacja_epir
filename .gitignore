node_modules
.env
.wrangler
dist
.dev.vars
*.log
**/service-account.json
# Lokalne zrzuty klucza SA (np. temp_sa.json) — nigdy nie commitować
temp_sa.json
.shopify
**/public/build
# Root package-lock.json jest wymagany przez CI (npm ci w .github/workflows/deploy-pages.yml).

# Sekrety – nigdy nie commitować
*-secrets.txt
bigquery-secrets.txt
chat-secrets.txt

# Cache buildów
**/.cache/
.npm-cache-local/

# Python & Local Data
.venv/
__pycache__/
*.pyc
# Azure local exports / experiments (dirname may appear as untracked file or folder)
Azure
data/

# Remix Build Artifacts (Hydrogen / Remix — generated by npm run build)
apps/*/functions/
apps/*/app/styles/tailwind-build.css

# Playwright / lokalne artefakty testów E2E (nie commitować)
test-results/
