{
  "name": "@epir/mcp-data-ops",
  "version": "0.1.0",
  "private": true,
  "description": "Lokalny MCP (stdio) — read-only audyt przepływu danych EPIR (D1, flow-health, sonda Q1).",
  "type": "module",
  "bin": {
    "epir-mcp-data-ops": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
